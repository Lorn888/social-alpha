import React from 'react';

export default function App() {
  const headingStyle = {
    fontFamily: 'Inter, sans-serif',
    fontWeight: 600,
    fontSize: '2rem',
    color: '#333',
  };

  return (
    <h1 style={headingStyle}>
      Hello world!
    </h1>
  );
}
